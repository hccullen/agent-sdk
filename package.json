{
  "name": "corti-agent-sdk-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace root for @corti/agent-sdk and its examples.",
  "workspaces": [
    "packages/js",
    "examples/ts"
  ],
  "scripts": {
    "build": "npm run build --workspace @corti/agent-sdk",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "overrides": {
    "@corti/sdk": "3.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.41"
  }
}
